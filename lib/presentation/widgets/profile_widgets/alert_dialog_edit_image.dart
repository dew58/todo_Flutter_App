import 'package:flutter/material.dart';

class AlertDialogEditImage extends StatelessWidget {
  const AlertDialogEditImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const AlertDialog();
  }
}
